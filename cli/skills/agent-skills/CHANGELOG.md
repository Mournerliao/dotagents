# Changelog

## 1.1.1 — 2026-08-18

- Catalog path in a git checkout is `cli/catalog.json`.

## 1.1.0 — 2026-08-18

- PATH binary is `agent-skills`. Catalog path in a git checkout is `cli/catalog/catalog.json`.

## 1.0.1 — 2026-08-18

- Default invocation is `npx --yes @mournerliao/agent-skills`. Catalog path in a git checkout is `cli/catalog/catalog.json`.

## 1.0.0 — 2026-08-13

- Initial maintained release. Replaces `find-skills-lite` with CLI operate + catalog discovery.
- Default invocation is `npx --yes @mournerliao/agent-skills`.
- Cursor consumes `~/.agents/skills` via `--agent codex --scope global`.
